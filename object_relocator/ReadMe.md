# Object Relocator
Tool for editing map, allow you to pick up, move and rotate an interactive object.

### v1.0
Initial Release.
### v1.1
Fixed quitting while in editor causing the editor keybind to no longer work.
### v1.2
Now should work on all interactive objects, activating the editor will iterate trought all objects and change their collision to allow trace function to work.
